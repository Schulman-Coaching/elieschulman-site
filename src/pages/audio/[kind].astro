---
// The public audio URLs are implemented by the three explicit pages under
// audio/{podcasts,lectures,interviews}. Returning no dynamic paths prevents
// those pages from being shadowed by a duplicate generated route.
export function getStaticPaths() {
  return [];
}
---
